CREATE TABLE goodguys (
  model varchar(30) PRIMARY KEY,
  retailer varchar(30),
  category varchar(30),
	currency varchar(30),
	brand varchar(30),
	name varchar(100),
	size varchar(30),
	price varchar(30),
	image varchar(200)
);
